/* test.sd

   Test all token types:
   - delimiters
   - operators
   - keywords
   - identifiers
   - string, int, real constants
   - comments
*/

// This is a single-line comment
const int count = 10;
double value = -3.14e+2;

// Identifiers and keywords
string message = "hello ""world""!";
bool flag = true;
char letter = 'A';

// Complex expressions
if (count >= 10 && value != 0.0) {
    count++;
    value = value / 2.5;
    println message;
}

// Nested loop
for (int i = 0; i < count; i++) {
    while (i < 5) {
        print "i is " "" i;
    }
}

// Array usage
int arr[5];
arr[0] = 123;
arr[1] = 456;

/* Multi-line comment
   with /* nested comment syntax */
   and symbols: + - * / < >= !=
*/

void main() {
    println "Done!";
}
